# System Performance Metrics & Evaluation Report
**Model(s):** ministral-14b-latest (14 cold queries), ministral-8b-latest (10 cold queries), rules (3 cold queries), gemini-3-flash-preview (1 cold query)
**Embeddings:** BAAI/bge-small-en-v1.5 (ONNX via fastembed)
**Environment:** 8 vCPU / 16 GB RAM / Darwin 27.0.0 (measurement machine)

_Generated by `eval/report.py` at commit `5d0dd12` on 2026-09-25 17:06 UTC. Template: Theme 2 spec, Appendix C. Every number comes from a run in `eval/results/`; anything without one says “not measured”._

## 1. Schema & Rule Compliance
Evaluated on sample datasets and held-out validation scenarios.

| Metric | Target | Measured Value |
| :--- | :--- | :--- |
| Schema-valid output lines | >= 99% | 100.0% |
| Rule compliance (Goal / Title / Description syntax) | >= 95% | 100.0% |
| Absolute URL leaks | 0 | 0 |
| Deeplink catalog validity (exact URI match) | 100% | 100.0% |
| Auto actions carrying valid actionable deeplink | >= 90% | 100.0% |

_135 of 135 responses had non-empty plans (gate replica, `5d0dd12`)._

## 2. Accuracy Benchmarks
Evaluated against reference ground truth scenarios across Battery, Display, Camera, and Performance.

| Evaluation Metric | Scale / Anchor | Score |
| :--- | :--- | :--- |
| Step accuracy (completeness, correctness, ordering) | 0.0 - 3.0 | 1.40 |
| Deeplink relevance (exact target screen vs. parent menu) | 0.0 - 2.0 | 1.80 |

_Step accuracy: gemini-3.5-flash-lite as judge (`eval/judge.py`, judge-v1) over 20 plans from results.jsonl. by set: kit 1.40 (n=20). 115 steps: 98 correct, 5 partial, 12 wrong (main issues: fragment 8, irrelevant 5, not an instruction 3). 9 plans with an ordering problem, 16 missing an article fix, 0 empty. end-to-end link relevance 0.00 / 2 over 2 catalog links._
_Deeplink relevance: the Screen Graph resolver on 123 hand-labelled gold steps (`data/gold/deeplink_gold.jsonl`), step by step rather than end to end. Precision@1 88.5% on the 87 steps with a real catalog answer. By labeller: karur 1.85 (n=87), nikhil 1.69 (n=36). The mapping lane tuned its thresholds on this file, so its own labels are the more optimistic half._

## 3. Latency Benchmarks (N >= 30 requests per path)

| Execution Path | Target (P95) | P50 (ms) | P95 (ms) |
| :--- | :--- | :--- | :--- |
| Cache hit - exact query match | <= 300 ms | 3.1 | 11.6 |
| Cache hit - unseen semantic paraphrase | <= 300 ms | 6.6 | 9.2 |
| Cold query - full pipeline extraction & mapping | <= 8000 ms | 5127.0 | 7045.0 |

_Measured over HTTP against a local API started on an empty cache, server-side `X-Latency-Ms` where the API sends it. Cache rows are timed on the calls that hit, the cold row on the calls that missed._
_7 of 35 cold-pass calls were answered from the cache (another request with the same article had just been solved, or the API did not start empty); cold latency is timed on the misses only_
_Samples: repeat n=40, paraphrase n=179 of 200, cold n=28 of 35._

## 4. Operational Cost & Cache Efficacy

| Metric Item | Target | Measured Value |
| :--- | :--- | :--- |
| Cold query average inference cost | Tracked | $0.0000 |
| Cache hit inference cost | $0.00 | $0.00 (no LLM call on the hit path, by construction) |
| Semantic cache hit rate (on unseen paraphrases) | >= 80% | 89.5% |
| Cost derivation method | - | (prompt tokens + completion tokens) × rate |

_Mean `meta.cost_usd` over 35 cold queries, priced from the token counts at the rates in `api/app/config.py` (`llm_prices`); models that answered: ministral-14b-latest 14, ministral-8b-latest 10, rules 3, gemini-3-flash-preview 1. Models without a listed rate run on a free plan and cost $0._
_Hit rate over 200 held-out paraphrases (`eval/sets/paraphrases.jsonl`, never used to warm the cache), 0 served the wrong plan._
_False hits on 60 near misses (same article, different problem): 1.7% (target <= 2%)._
_Counted as false hits: near misses served a kit answer (1 their own row's, 0 another row's with the same article). The live pass also caches every answer it computes, so a near miss can hit an answer created earlier in the same test; those are reported, not counted: paraphrase answer 3, earlier near miss 19._

## 5. Architectural Ablation Analysis

| Architecture Variant | Step Accuracy | Latency (P95) | Cost / Query | Key Observations |
| :--- | :--- | :--- | :--- | :--- |
| Baseline: Full LLM Deeplink Mapping | held fixed (mapping-only) | 8017.3 ms / step | $0.00175 | deeplink relevance 1.42/2, P@1 71.3%, right tier 80.5%, wrong or unsafe link on 17.9% of steps |
| Variant A: Hybrid BM25 + Dense Embedding Retrieval | held fixed (mapping-only) | 5.0 ms / step | $0.00 | deeplink relevance 0.89/2, P@1 60.9%, right tier 70.7%, wrong or unsafe link on 53.7% of steps |
| Variant B: Pure Rules-Based Deeplink Mapping | held fixed (mapping-only) | 0.8 ms / step | $0.00 | deeplink relevance 1.57/2, P@1 75.9%, right tier 87.0%, wrong or unsafe link on 17.1% of steps |
| Extra: BM25 keyword retrieval only | held fixed (mapping-only) | 0.5 ms / step | $0.00 | deeplink relevance 1.00/2, P@1 67.8%, right tier 71.5%, wrong or unsafe link on 48.8% of steps |
| Ours: Screen Graph resolver (hybrid + polarity + tiers) | held fixed (mapping-only) | 4.4 ms / step | $0.00 | deeplink relevance 1.80/2, P@1 88.5%, right tier 91.9%, wrong or unsafe link on 8.9% of steps |

_Mapping-only ablation: every variant maps the same 123 gold steps (87 catalog, 22 dummy, 14 manual), so extraction and therefore step accuracy are held fixed; the variants differ in deeplink relevance (0-2 rubric in `eval/evalkit/relevance.py`), precision@1 and safety. Commit `5d0dd12`._

## 6. Known Edge Cases & System Limitations

* **Catalog gaps.** Software update, Safe mode, Dark mode, auto-rotate, font size, per-app storage and a true Factory data reset have no catalog entry, so those steps resolve to `bixby://dummy_positive` or stay manual. `DL-0022` is the *auto* factory reset, not Factory data reset.
* **Dummy rate by catalog gap.** 22 of 123 gold steps need a Settings screen the catalog lacks, and 14 are physical steps that take no link.
* **Over-cautious links.** 7 steps with a real catalog entry fell back to the placeholder because the match scored under the catalog floor: Settings > Battery > Charging; Settings > Battery and device care > Optimize now; Settings > Display > Navigation bar; Settings > Notifications > Battery percentage; Settings > Security and privacy > Other security settings > Pin windows; Settings > Sounds and vibration > Volume > Alarm; Settings > Sounds and vibration > Volume > Media.
* **Near-miss cache hits.** 1 of 60 near misses were served a kit answer (intent: 1): "I want to add a floating circle to my Galaxy S25 screen that gives me quick shortcuts to recent apps, home, back, screen off and volume control.".
